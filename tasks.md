# tasks.md -- pm-zero v12 Execution Ledger

## Goal Binding
- Vision source: docs/vision.md
- Active goal: Maintain Grimoire (formerly Task Plant) under pm-zero v12 with product code changes tracked as explicit tasks.
- Main agent: Claude Code (Sonnet-first) — sole owner of this ledger.
- Implementation: Claude Code main agent; Sonnet worker subagents on disjoint scopes.
- Review: fresh-context reviewer subagent (Opus 5, Tier 1) for large/behaviour-changing/hard-to-undo diffs; Tier 2 retired in v12.
- Historical rows below owned "Codex CLI" under pm-zero v9.4; kept as-is for evidence integrity.

## Status Vocabulary
- proposed: idea exists, not ready
- ready: owner, dependencies, write scope, acceptance, verification, and expected evidence are clear
- doing: one owner is actively working
- blocked: needs decision, dependency, credential, environment, or human action
- review: implementation complete, review pending
- done: accepted by reviewer
- verified: evidence recorded

## Parallelization Rules
- The main agent owns tasks.md.
- Worker subagents own only their assigned Write Scope.
- Parallel implementation requires disjoint Write Scopes or worktree isolation.
- If two tasks need the same file, serialize them.
- Default cap: <=2 concurrent worker subagents on a Pro plan.
- Subagents return reports; the main agent updates tasks.md.

## Tasks
| ID | Status | Owner | Depends On | Write Scope | Acceptance | Verification | Evidence |
|---|---|---|---|---|---|---|---|
| T001 | verified | Codex CLI | none | Legacy pm-zero metadata and project docs | Repository had the earlier task ledger, repo map, scope lock rules, aligned commands, and pnpm-only lockfile policy | git diff --check; pnpm lint; pnpm typecheck; pnpm test; pnpm build; pnpm verify | all passed on 2026-05-15 |
| T002 | done | Codex CLI | T001 | Existing implementation files for active product work only | Continue Task Plant Phase 1-7 using docs/implementation-plan.md with disjoint write scope per task | pnpm lint; pnpm typecheck; pnpm build; related tests; browser smoke for UI | 2026-07-19: closed as superseded — all implementation-plan phases shipped across T003-T020; no remaining work under this row |
| T003 | verified | Codex CLI | T001 | src/app/plant/page.tsx, src/components/navigation/bottom-nav.tsx, src/components/plant/, src/hooks/use-plant.ts, src/lib/domain/plant.ts, tests/lib/domain/plant.test.mjs, docs/decisions.md, docs/repo-map.md, docs/state.md, tasks.md | Plant tab is visible on app screens, `/plant` renders the monthly plant view, and domain tests prevent blank plant state regressions | pnpm lint; pnpm typecheck; pnpm test; pnpm build; pnpm verify; browser smoke | all passed on 2026-05-15; smoke confirmed `/`, `/plant`, `/all` show 3-tab nav and `/plant` shows バラ |
| T004 | verified | Codex CLI | T003 | src/lib/domain/plant.ts, src/hooks/use-plant.ts, src/lib/taskDb.ts, src/hooks/use-home-screen.ts, src/components/all/all-screen.tsx, src/components/navigation/bottom-nav.tsx, src/components/home/home-screen.tsx, src/components/plant/plant-screen.tsx, tests/lib/domain/plant.test.mjs, docs/decisions.md, docs/state.md, tasks.md | Review指摘のNB-1〜NB-4を解消し、週境界・植物同期・採番重複の再発を防止する | pnpm lint; pnpm typecheck; pnpm test; pnpm build; pnpm verify | all passed on 2026-05-15; week-start tests added; plantState sync added for / and /all update paths |
| T005 | verified | Codex CLI | none | AGENTS.md, CLAUDE.md, HANDOFF-JA.md, tasks.md, docs/state.md, docs/repo-map.md, docs/decisions.md, scripts/setup.mjs, scripts/verify.mjs, .claude/settings.json, .gitignore | pm-zero v9.4 source-of-truth files are current, old project-local hook/MCP/Codex scaffolds are removed unless justified, and product code is untouched | git diff --check; pnpm verify | 2026-05-17: node scripts/verify.mjs passed; git diff --check passed before commit. |
| T006 | verified | Codex CLI | T003 | docs/plant-reward-image-prompts.md, docs/decisions.md, docs/state.md, tasks.md | 12か月の植物ごとに、自然写真として破綻しない報酬画像プロンプトを5案ずつ定義し、生成前に選定基準を固定する | git diff --check | 2026-05-17: prompt manifest added for 12 flowers x 5 variants; git diff --check passed |
| T007 | verified | Codex CLI | T003 | public/plant-rewards/, docs/plant-reward-image-sources.md, docs/state.md, tasks.md | Pexels/Unsplash/Pixabay等の無料利用可能な写真から12か月の花候補を収集し、元URL・ライセンス参照・保存先を記録する | source manifest check; git diff --check | 2026-05-17: 12 Unsplash candidates downloaded; source manifest and dimensions recorded; git diff --check passed |
| T008 | verified | Codex CLI | T007 | public/plant-rewards/candidates-v2/, docs/plant-reward-image-sources-v2.md, docs/plant-reward-image-selection-research.md, docs/state.md, tasks.md | Web/SNS由来の美しい花写真の判断軸を調査し、花全体像が見える候補を12花×5枚収集して出典・ライセンス・保存先を記録する | source manifest check; git diff --check | 2026-05-17: 60 candidates downloaded; 5 images per monthly flower confirmed; selection research and source manifest recorded; git diff --check passed |
| T009 | verified | Codex CLI | T008 | public/plant-rewards/candidates-v2/, docs/plant-reward-image-sources-v2.md, docs/plant-reward-image-selections.md, docs/state.md, tasks.md | 桜だけ別テイストの追加候補を5枚収集し、出典・ライセンス・保存先を記録する | source manifest check; git diff --check | 2026-05-17: sakura candidates 6-10 downloaded; selected candidates recorded except month 3; git diff --check passed |
| T010 | verified | Codex CLI | T009 | public/plant-rewards/candidates-v2/, docs/plant-reward-image-sources-v2.md, docs/state.md, tasks.md | 桜のややアップ寄り候補を追加収集し、出典・ライセンス・保存先を記録する | source manifest check; git diff --check | 2026-05-17: close-up sakura candidates 11-15 downloaded; source manifest updated; git diff --check passed |
| T011 | verified | Codex CLI | T010 | src/components/plant/, src/lib/domain/plant.ts, tests/lib/domain/plant.test.mjs, public/plant-rewards/, docs/plant-reward-image-selections.md, docs/plant-reward-image-sources.md, docs/repo-map.md, docs/state.md, tasks.md | 選定済み12枚の花写真を植物画面へ反映し、開花までを早め、未採用画像と候補情報を削除する | pnpm lint; pnpm typecheck; pnpm test; pnpm build; pnpm verify; browser smoke; git diff --check | 2026-05-17: final 12 reward photos added; candidates removed; blooming starts at 5 weekly completions; lint/typecheck/test/build/verify/diff-check passed; browser smoke confirmed photo reward with no console errors |
| T012 | verified | Codex CLI | T011 | src/components/navigation/bottom-nav.tsx, src/components/plant/plant-screen.tsx, docs/state.md, tasks.md | 開花写真表示中でもスマホの下部ナビでホーム/カレンダーへ遷移できる | pnpm lint; pnpm typecheck; pnpm test; pnpm build; pnpm verify; browser mobile smoke; git diff --check | 2026-05-17: reward image and overlay made non-interactive; bottom nav given explicit z-index; mobile 390x844 smoke confirmed /plant photo state can navigate to / and /all with no console errors |
| T013 | verified | Codex CLI | T012 | src/hooks/use-home-screen.ts, tasks.md | Home screen initialization keeps the same behavior while React hook dependencies are explicit and the exhaustive-deps suppression is removed | pnpm lint; pnpm typecheck; pnpm test; pnpm build | 2026-05-17: initial lint exposed react-hooks/set-state-in-effect; load moved behind a microtask; lint/typecheck/test/build passed. |
| T014 | verified | Codex CLI | T013 | src/lib/gemini.ts, src/components/home/voice-input-button.tsx, docs/state.md, tasks.md | 音声タスク登録でGemini APIが400を返しても、リクエスト形式/モデルのフォールバックで復旧可能であり、空音声入力も安全に扱える | pnpm lint; pnpm typecheck; pnpm test; pnpm build; pnpm verify | 2026-05-25: gemini client fallback (payload/model) added; transcript empty guard added; lint/typecheck/test/build/verify passed. |
| T015 | verified | Claude Code | none | CLAUDE.md, .claude/settings.json, HANDOFF-JA.md, docs/state.md, docs/repo-map.md, docs/decisions.md, tasks.md, scripts/verify.mjs; removed AGENTS.md, OS-KERNEL.md, MEMORY.md, docs/codex-prompt.md | Non-source governance files match pm-zero v11: self-contained CLAUDE.md, bypassPermissions project settings, Codex/AGENTS artifacts removed, tiered self-review documented; product source untouched | pnpm verify; git diff --check | 2026-07-19: node scripts/verify.mjs all checks passed (lint/typecheck/test/build); git diff --check clean; product source unchanged. |
| T016 | verified | Claude Code | T015 | src/app/globals.css, src/app/layout.tsx, src/app/manifest.ts, src/lib/domain/category.ts, src/lib/view-transition.ts (new), src/hooks/use-home-screen.ts, src/components/home/, src/components/navigation/, src/components/plant/plant-screen.tsx, src/components/all/, src/components/gmail/gmail-import-modal.tsx, src/components/calendar/calendar-import-modal.tsx, docs/decisions.md, docs/state.md, tasks.md, HANDOFF-JA.md | 2026 design overhaul: media-driven dark-mode-first oklch token system wired through every screen (no hardcoded light-only palette classes left), WCAG 2.2 fixes (pinch-zoom enabled, >=24px targets, progressbar roles, reduced-motion honored), home hero card with animated progress ring + streak chip + plant mini-widget, task-card micro-interactions (SVG check draw, animated strikethrough, grid-rows expand, native View Transitions list FLIP, overdue signal), polished nav/FAB; domain logic and data layer untouched | pnpm verify (lint, typecheck, test, build); git diff --check | 2026-07-19: pnpm verify all green (lint clean, typecheck clean, 10/10 domain tests, build 5 static routes); hardcoded palette sweep confirmed via grep (remaining hits are scheme-independent photo overlays and scrims) |

| T017 | verified | Claude Code | T016 | src/lib/domain/plant.ts, src/lib/domain/drops.ts (new), src/lib/db.ts, src/lib/taskDb.ts, src/lib/rewardDb.ts (new), src/lib/sound.ts (new), src/hooks/use-plant.ts, src/hooks/use-home-screen.ts, src/components/reward/ (new), src/components/book/ (new), src/app/book/ (new), src/components/home/, src/components/navigation/bottom-nav.tsx, src/components/plant/plant-screen.tsx, src/app/layout.tsx, src/app/globals.css, src/app/manifest.ts, package.json (+next-view-transitions), pnpm-lock.yaml, tests/, docs/, tasks.md | Iceborne-inspired survey-base reward system (no Capcom IP embedded): task completion rolls a material drop (RARE1/4/8, pity 12, first-of-day floor RARE4) recorded in a survey-notes collection at /book; plant growth becomes cumulative monthly (weekly reset abolished) with Dexie v3 additive migration preserving all data; Web-Audio pentatonic sound system + haptics with persistent mute; page-turn route transitions; frost+ember palette and quest vocabulary; one consistent squish press effect | pnpm verify (lint, typecheck, test, build incl. new domain tests); git diff --check | 2026-07-19: pnpm verify all green (lint/typecheck clean, 16/16 tests, build 6 static routes incl. /book); Tier 2 Opus review PASS with 0 blockers; 3 findings fixed (&[taskId+dateKey] unique index, page-turn timer race, audio priming on first gesture) and re-gated with lint/typecheck/test |
| T018 | verified | Claude Code | T017 | src/lib/domain/bounty.ts (new), src/lib/domain/streak.ts (new), src/lib/departure.ts (new), src/lib/rewardDb.ts, src/lib/taskDb.ts, src/hooks/use-home-screen.ts, src/components/home/bounty-board.tsx (new), src/components/home/home-screen.tsx, src/components/home/task-card.tsx, tests/, docs/, tasks.md | Stage 2 of the staged reward plan: deterministic daily bounty board (3 achievable-anytime micro-missions: 出発1件 / 達成N件 / 登録1件) auto-claiming a bonus drop through the existing grant pipeline (drops unique index doubles as the claim ledger — no DB v4), a 出発 (departure) button on task cards targeting task initiation (per-day localStorage, ephemeral by design), and streak insurance (pure domain calc: today-pending grace + 1 freeze day per chain) | pnpm verify incl. new bounty/streak domain tests; git diff --check | 2026-07-20: pnpm verify all green (lint/typecheck clean, 27/27 tests incl. 11 new bounty/streak cases, build 6 routes); Tier 1 fresh Sonnet review pass after fixes (see Review Notes) |
| T019 | verified | Claude Code | T017 | package.json, src/app/layout.tsx, src/app/manifest.ts, public/icon.svg, public/icons/, scripts/gen-icons.mjs, scripts/verify.mjs, README.md, CLAUDE.md, CONTEXT.md, docs/decisions.md, docs/state.md, tasks.md, HANDOFF-JA.md, GitHub repo rename (snowtone-ai/task-plant -> snowtone-ai/grimoire) | Product renamed to "Grimoire" across live surfaces (package name, PWA display name incl. homescreen label, README, governance docs, verify banner, GitHub repo); historical ledger/decision/implementation-plan texts stay untouched for evidence integrity; new frost-grimoire app icon (hand-crafted SVG) regenerated into all 4 PNG variants via the existing sharp pipeline (sharp added as devDependency) | pnpm verify; node scripts/gen-icons.mjs succeeds; gh repo view shows new name; git diff --check | 2026-07-19: pnpm verify all green after rebrand; gen:icons regenerated 4 PNGs (512 + maskable visually confirmed, full-bleed bg fix applied); gh repo view snowtone-ai/grimoire returns name=grimoire; remote URL updated |

| T020 | verified | Claude Code | T019 | src/app/globals.css, src/app/layout.tsx, src/components/home/home-screen.tsx, src/components/all/all-screen.tsx, src/components/plant/plant-screen.tsx, src/components/book/book-screen.tsx, src/components/navigation/bottom-nav.tsx, src/components/reward/drop-reveal.tsx, src/components/home/task-add-modal.tsx, src/components/home/task-edit-modal.tsx, docs/decisions.md, docs/state.md, tasks.md, HANDOFF-JA.md | Iceborne-vista frontend pass grounded in researched art direction: app-wide fixed atmosphere layer (aurora + violet fringe + below-horizon hearth glow over a night-blue gradient; daylight snowfield in light scheme), Cinzel display font for overlines/QUEST CLEAR banner via next/font, gold hairline accents on hero/nav/book sections, ember-gradient primary CTAs, gradient progress ring, gentle CSS snowfall on the lab screen (reduced-motion hidden); screen roots turn transparent so the sky shows through; no logic/DB changes | pnpm verify; git diff --check | 2026-07-19: pnpm verify all green (lint/typecheck clean, 16/16 tests, build 6 routes with Cinzel font); diff +197/-23 (<300, presentation-only) so Tier 0 gate applies per CLAUDE.md review classes |

| T021 | verified | Claude Code | T018 | src/lib/backup.ts (new), src/components/book/book-screen.tsx, docs/, tasks.md | JSON backup: export downloads a versioned snapshot of tasks/streaks/plantState/drops; import validates the shape and upserts by primary key inside one transaction with an explicit in-UI confirmation, making future origin migrations data-safe (closes the vision backlog item) | pnpm verify; git diff --check | 2026-07-20: pnpm verify all green; import hardened after Tier 1 review (drops shape validation, BACKUP_VERSION guard); shipped in the same PR as T018 |
| T023 | verified | Claude Code | T022 | src/lib/domain/chronicle.ts (new), src/lib/rewardDb.ts, src/components/book/book-screen.tsx, tests/lib/domain/chronicle.test.mjs, docs/ | Seasonal chronicle for permanent-use survey notes (D-028): /book gains a CHRONICLE section — a live "this month's subject" page (the month's flower) plus permanently-accumulating past-month cards (討伐/希少/活動日), all derived from the never-revoked drops table via a pure buildChronicle; no DB migration. Directly targets the user's habituation concern with monthly novelty + unbounded accumulation | pnpm verify; browser smoke; git diff --check | 2026-07-21: pnpm verify green (chronicle 4/4 tests); browser smoke confirmed current-month hero (2026年7月ひまわり 討伐9/希少5/活動7日) + past-month cards (6月/5月) render from seeded drops |
| T024 | verified | Claude Code | T023 | src/lib/domain/drops.ts, src/lib/sound.ts, src/hooks/use-home-screen.ts, src/components/reward/drop-reveal.tsx, src/components/book/book-screen.tsx, tests/lib/domain/drops.test.mjs, docs/ | Honest RARE 1-8 rarity ladder (D-029): expand from the gapped 1/4/8 to a full 8-rank scale (game research: MHW has items at every rank). Existing drops stay at their ranks (backward compatible); ranks 2/3/5/6/7 get 29 new frozen-survey materials (catalog 36→63). Gacha-curve probabilities (8:2.5%…1:28%), pity+first-of-day floor kept. Reveal/sound/confetti bucket 8 ranks into low(1-3)/mid(4-6)/high(7-8) per the 1-action-1-sound rule; /book renders 8 config-driven sections with an ascending badge ramp | pnpm verify; browser smoke; git diff --check | 2026-07-21: pnpm verify green (36/36 tests incl. full 1-8 band coverage + no-gap ladder); browser smoke confirmed all RARE 1-8 sections render with no gaps and ascending badge colors, 0 console errors |
| T022 | verified | Claude Code | none | src/lib/domain/task-date.ts, src/components/all/calendar-view.tsx, src/components/all/all-screen.tsx, tests/lib/domain/task-date.test.mjs, removed src/components/all/all-constants.ts, docs/decisions.md, docs/state.md, tasks.md | Survey-log calendar heatmap: after category dots were removed the calendar showed nothing on days with tasks; make the FIELD MAP the time-axis view of the 調査記録. Past/today cells glow with ember heat scaled by completions-that-day (tier 0-3, capped 42% for contrast), pending days show a frost dot (restored task-presence affordance), and a SURVEY LOG stats strip (討伐/活動日/予定 + 通算) gives the screen standalone value. No-punishment: past incompletes are not marked red. Pure domain helpers (buildCalendarSummary/summarizeCalendarMonth/completionHeatLevel) unit-tested; dead buildCategoryDotMap + empty all-constants.ts removed. No DB migration | pnpm verify; browser smoke (seeded IndexedDB); git diff --check | 2026-07-21: pnpm verify all green (lint/typecheck clean, 31/31 tests incl. 4 new calendar cases, build 6 routes); browser smoke confirmed 3-tier ember heat, frost pending dots, today ring, and SURVEY LOG stats (討伐11/活動4日/予定3/通算11) render correctly with readable day numbers; diff +229/-13 (<300, presentation + pure domain) so Tier 0 gate applies per CLAUDE.md review classes |
| T025 | verified | Claude Code | none | src/app/api/gemini/generate/route.ts (new), src/lib/gemini.ts, src/lib/api/gemini-prompts.ts, src/lib/api/gmail-task-extractor.ts, .env.example, README.md, docs/decisions.md, docs/state.md, tasks.md | Enable family (non-developer) multi-user access: architecture audit found tasks/plant/drops/Google-OAuth tokens already fully local per-device (no code change needed there) and Vercel SSO protection was blocking the production URL entirely, so it was scoped to preview-only via Vercel project settings (outside git). That makes production public, so NEXT_PUBLIC_GEMINI_API_KEY (previously safe behind SSO) had to move server-only through src/app/api/gemini/generate, which after Tier 2 review was hardened from a free-form prompt relay into two fixed typed request shapes. PR #19 ready and Tier 2-reviewed, but blocked on: (a) explicit human merge confirmation (security/deploy class, CLAUDE.md high-risk gate), (b) human setting GEMINI_API_KEY in Vercel env vars, (c) human checking the Google OAuth consent screen test-user list | pnpm lint; pnpm typecheck; pnpm test; pnpm build; manual curl smoke of /api/gemini/generate (both kinds + invalid bodies); git diff --check | 2026-08-15: pnpm lint/typecheck/test(36/36)/build all green after the Tier 2 fix round; curl smoke confirmed 400 on invalid/empty bodies, controlled 502 (no key/upstream-detail leak) on upstream failure for both kind=voice and kind=gmail; PR #19 open on feat/gemini-server-proxy; Vercel ssoProtection set to deploymentType=preview (prod confirmed publicly reachable via curl 200); Tier 2 Opus fresh-context review: pass after fixes (see Review Notes) |
| T026 | verified | Claude Code | T025 | src/lib/domain/drops.ts, src/components/book/book-screen.tsx, src/lib/rewardDb.ts, src/lib/db.ts, tests/lib/domain/drop-catalog.test.mjs (new), tests/lib/reward-db.test.mjs (new), package.json, pnpm-lock.yaml, docs/decisions.md, docs/state.md, tasks.md | Reward catalog expanded ~5x (63→308) for long-term daily use, with RARE4 gaining 36 per-month specimen variants behind a new RARE4_POOL while RARE_DROPS stays the 12 canonical species that book-screen's month→emoji map depends on; RARE8 stays 12 (photo-backed). All 63 legacy drop ids preserved byte-identical and pinned by test, since they are foreign keys in the user's IndexedDB ledger. Also removes the full-table db.drops.toArray() scan from the task-completion hot path (it grew with the append-only ledger for the life of the install) in favour of indexed queries, and makes getDropById a Map lookup now that the catalog is 5x. fake-indexeddb added so the real Dexie write path is executed in tests rather than only typechecked. See D-031 | pnpm verify (lint, typecheck, 55 tests, build); node --test tests/lib/reward-db.test.mjs; measured grant cost at 8k records | 2026-08-15: pnpm verify all green (lint clean, typecheck clean, 55/55 tests, build 7 routes). Catalog verified 308 entries, 308 unique ids, 308 unique names, pool total == DROP_CATALOG length. Reward path measured at 8000 records: 9ms indexed vs 45ms for the old full-scan equivalent, gap widening linearly. Tier 2 Opus pre-merge review in progress — not yet merged |

| T027 | done | Claude Code | T026 | src/lib/domain/regions.ts (new), src/lib/domain/reward-catalog/ (new, 8 files), src/lib/domain/drops.ts, src/components/book/book-screen.tsx, src/lib/rewardDb.ts, tests/lib/domain/drop-catalog.test.mjs, tests/lib/reward-db.test.mjs, docs/decisions.md, docs/state.md, tasks.md | Reward catalog rebuilt from a single "frozen tundra" motif into an 8-region world atlas (frost/aegis/caravan/canopy/lantern/grove/savanna/tide, each an original real-culture+fantasy fusion, D-032). Catalog grows 308→436 (8 regions x 47 items across ranks 1/2/3/5/6/7, user-authorized backward-compat break — old ids not preserved); RARE4/RARE8 (season-linked, photo-backed) stay byte-identical (ids/names/photos) on a separate "garden" axis for mechanical reasons (chronicle month-map, real photo files). /book gains an EXPEDITIONS overview section and CSS-only foil/gradient material cards (region accent x item color, no new image assets — AI-generated illustrations for RARE6-8 were requested but are infeasible in this environment: no image-gen tool/API key access, documented in D-032 as a future task) | pnpm verify (lint, typecheck, 58 tests, build); browser smoke (seeded IndexedDB across 8 regions x multiple ranks) | 2026-08-15: pnpm verify all green (lint clean, typecheck clean, 58/58 tests incl. rewritten catalog-structure tests + pinned garden-id test, build 7 routes). Catalog verified 436 entries across RARE1-8 (88/80/72/48/56/48/32/12), 0 duplicate ids/names. Browser smoke on /book confirmed EXPEDITIONS region cards, gradient/foil material cards (visually distinct per region color, especially RARE6-8), Chronicle/RARE8 vista section unaffected, 0 console errors (2 pre-existing unrelated Next.js LCP hints only). Tier 2 fresh-context Opus review: PASS WITH NITS, 2 major + 9 minor/nit findings, see Review Notes. All majors and cheap minors/nits fixed in a follow-up commit; re-verified green. Merged to main, PR #20 closed, branch deleted. |

| T028 | done | Claude Code | none | package.json, pnpm-lock.yaml, docs/decisions.md, docs/state.md, tasks.md | All 62 open Dependabot alerts on the default branch (25 high/33 moderate/4 low, discovered via a push notice after PR #20) are resolved: 0 open after the fix (D-033). shadcn's @modelcontextprotocol/sdk->hono/express chain (~30 alerts) pinned via scoped pnpm.overrides rather than removed (shadcn/tailwind.css turned out to be load-bearing for the app's design tokens, confirmed by an actual build break when the dependency was dropped); next bumped 16.2.11->16.3.1 (and eslint-config-next to match) resolves postcss/nanoid/sharp natively since 16.3.1 already declares the patched versions itself, avoiding an override that would have exceeded next's own declared semver range; the remaining dev/build-tool-only transitive vulns (brace-expansion x3 major lines, fast-uri, js-yaml, serialize-javascript, @babel/core, @babel/plugin-transform-modules-systemjs) pinned via pnpm.overrides, scoped-by-major-range where multiple incompatible major lines coexist and bare/global only where confirmed single-major-line | pnpm audit (0 vulnerabilities); pnpm verify (lint, typecheck, 58 tests, build); manual next start + curl smoke of /_next/image (exercises the bumped sharp version end-to-end) | 2026-08-15: pnpm audit clean (was 62, now 0). pnpm verify all green. next start + curl smoke of /_next/image?url=... returned 200 image/jpeg (resized), confirming sharp@0.35.3 actually works at runtime, not just at build time. Tier 2 Opus fresh-context review: PASS WITH NITS; 4 pre-merge-recommended findings fixed (see Review Notes), re-verified green. PR #21 merged to main, branch deleted. |

| T029 | done | Claude Code | none | src/lib/taskDb.ts, src/lib/rewardDb.ts, src/components/book/book-screen.tsx, tests/lib/task-db.test.mjs (new), tests/lib/reward-db.test.mjs, docs/decisions.md, docs/state.md, tasks.md | Calendar reset (deletes all `tasks`+`streaks`) and survey-notes reset (deletes all `drops`) added as two buttons in a new RESET card on /book, next to the existing ARCHIVE backup card (D-034). Neither table has independent state of its own (calendar/book are both fully derived views per D-027/D-032), so scope was clarified with the user via AskUserQuestion before implementing: calendar = tasks+streaks only, survey notes = drops only, plantState untouched by either. Destructive action gated by a two-tap in-app confirm (count-backed warning panel -> explicit "本当に削除する"), matching the existing quest-delete pattern in task-edit-modal.tsx; no native window.confirm, no forced auto-backup (per user's explicit choice) | pnpm verify (lint, typecheck, 62 tests, build); browser smoke (seeded IndexedDB, both reveal/cancel and reveal/confirm flows for both buttons) | 2026-08-15: pnpm verify all green (lint/typecheck clean, 62/62 tests incl. 4 new reset-specific cases across 2 files, build 7 routes). Fixed a latent bug surfaced by the new test: taskDb.ts's own relative imports lacked `.ts` extensions (worked under webpack, broke under Node's native test-runner type stripping) — added `.ts` to all 4. Browser smoke via Playwright (after clearing a stale-Service-Worker false alarm unrelated to this change) confirmed: reveal shows an accurate count ("クエスト14件・ストリーク記録0件"/"ドロップ記録43件"), cancel is a true no-op, confirm deletes and shows "削除しました" with 0 console errors, home/all screens show empty state afterward, book screen collection/chronicle drop to 0/436 after survey reset. Tier 1 fresh-context Sonnet review: 1 real bug found and fixed (see Review Notes), re-verified green. Merged to main, PR #22 closed, branch deleted. |

| T030 | verified | Claude Code | none | ~/.claude/CLAUDE.md, ~/.claude/settings.json, ~/.claude/hooks/guard.mjs, CLAUDE.md, .claude/settings.json, .claude/rules/tests.md (new), docs/decisions.md, tasks.md, docs/state.md; removed a stale cross-project auto-memory entry | pm-zero v11 -> v11.1.1 governance migration applied to both global (~/.claude, affects all of the user's other repos) and project scope per D-035: P1 subagent spawn-depth cap, P2 RTK removed (unverified compression claim), P3 CLAUDE_CODE_AUTO_COMPACT_WINDOW absolute-token override, P4 Opus 5 references, P5 explicit allow:["*"] under bypassPermissions, P7 guard.mjs now blocks .env writes via Edit/Write/MultiEdit/NotebookEdit (previously Read-only), A2 PreCompact checkpoint-commit hook, A3 StopFailure(rate_limit\|overloaded) checkpoint hook, A4 .claude/rules/*.md path-scoped lesson files, A5 version re-verification trigger, A6 "measure the paired bill" principle; project .claude/settings.json's dead `"Bash(rm -rf:*)"` deny rule (invalid colon syntax, never matched) fixed to `"Bash(rm -rf *)"`; product source untouched | manual JSON validation of both settings.json files; manual guard.mjs read-through confirming Read/Edit/Write/MultiEdit/NotebookEdit all guard .env/.env.* identically; grep confirming zero remaining RTK references in either CLAUDE.md; git diff --check | 2026-08-15: both settings.json files parse as valid JSON; guard.mjs re-read in full, Edit/Write/MultiEdit/NotebookEdit branch mirrors the Read branch's regex exactly; grep for RTK/"Rust Token Killer" across both CLAUDE.md files returns 0 matches; git diff --check clean; no product source files touched (grep of the diff confirms only docs/config/governance paths). |

| T031 | verified | Claude Code | none | src/components/reward/drop-reveal.tsx, src/lib/domain/reminders.ts (new), src/lib/notifications.ts, public/sw.js, src/hooks/use-home-screen.ts, src/components/home/home-screen.tsx (通知バナーの文言のみ), tests/lib/domain/reminders.test.mjs (new), docs/decisions.md, docs/state.md, tasks.md | VoC分析 (D-036) で「要望」ではなく不具合と判定された2件を修正する。(1) ドロップカードの自動消滅が 2000-3400ms しかなく、7要素(QUEST CLEAR/画像/レア度/NEW/名前/flavor/記録メッセージ)を読み切れない。さらにオーバーレイ全体の onClick={onDismiss} がカード本体も含むため「読もうとして触ると消える」。尺を 4000-9000ms へ引き上げ、カード本体のタップを stopPropagation し、明示的な閉じるボタンを置く。(2) 締切通知が構造的に発火しない: sw.js の setTimeout は SW が数十秒のアイドルで停止した時点でタイマーごと破棄され、notifications.ts の `if (todayAt9 > now)` により朝9時以降に開いた日は翌日分も含め一切スケジュールされない。起動時キャッチアップ配信へ作り替え、9時以降の起動でも取りこぼさないようにする。設定項目にはしない(壊れているものは直す、が D-036 の判別基準) | pnpm verify (lint, typecheck, test, build); 新規 notifications ドメインテスト; browser smoke | 2026-08-16: pnpm verify all green (80/80 tests)。reminders ドメインテスト8件を新規追加、うち1件は今回の本丸の回帰ガード（`selectDue still fires when the app is first opened long after 09:00`）で、旧実装が夜22時起動時に翌日分すら積まなかった挙動を固定。リプレイ非自動消滅と閉じる導線は T033 の browser smoke で併せて確認。 |
| T032 | verified | Claude Code | T031 | src/app/settings/page.tsx (new), src/components/settings/settings-screen.tsx (new), src/components/home/home-screen.tsx, src/components/book/book-screen.tsx, src/components/navigation/bottom-nav.tsx, src/hooks/use-home-screen.ts, docs/decisions.md, docs/state.md, tasks.md | 設定画面 /settings を新設し、ホームと /book に散らばっていた設定UIを集約する (D-036)。現状ホームには FxToggle がヘッダに常駐し、NotificationPanel の「🔔 通知が有効です + テスト通知を送る」が閉じるまで今日のクエスト一覧の上に居座っており、演出の議論とは無関係に F-1「開いた瞬間に今日やることを把握」を毎日阻害している。/settings に「音とバイブ」「通知」「データ」の3セクションを作り、FxToggle・NotificationPanel・/book の ARCHIVE(T021)・RESET(T029) を移設する。ヘッダの FxToggle が空いた位置に歯車を置くのでホームのアイコン数は増えない。BottomNav は4項目のままで /settings はサブページ(戻る導線つき、アクティブ項目なし)。副作用対処: 通知許可を求める導線がホームから消えるため、F-7 Must NOT「初回起動時に即表示してはいけない(価値を体感した後に許可を求める)」を守りつつ、初回の全クエスト達成直後に1度だけホームで許可を提示し、以降は /settings からのみとする | pnpm verify (lint, typecheck, test, build); browser smoke (/settings 5セクション、ホーム/book からの撤去、初回全完了プロンプトの1度きり動作) | 2026-08-16: pnpm verify all green、build に /settings ルートが出現（7→8 routes）。Browser smoke で /settings に ARCHIVE のエクスポート/インポートと RESET の2ボタン（カレンダー/調査記録）が移設済みであること、/book 側からは消えていることを確認。ホームヘッダは FxToggle が歯車(/settings リンク)に置き換わりアイコン数は据え置き。BottomNav は4タブのままで /settings では aria-current が付かない。 |
| T033 | verified | Claude Code (+2 Sonnet workers on disjoint scopes) | T032 | src/lib/domain/fx.ts (new), src/lib/fx.ts (new), src/lib/confetti.ts (new, extracted from use-home-screen), src/lib/spark.ts (new), src/lib/sound.ts, src/hooks/use-home-screen.ts, src/components/settings/settings-screen.tsx, src/components/book/book-screen.tsx, src/components/reward/drop-reveal.tsx, src/components/home/home-screen.tsx, src/components/pwa-register.tsx, src/app/globals.css, tests/lib/domain/fx.test.mjs (new), docs/decisions.md, docs/state.md, tasks.md | 演出強度プリセットと「接触の報酬」を実装する (D-036)。VoC の本質課題は「達成の報酬は作ったが接触の報酬を作っていない」で、演出発火点が事実上タスク完了1箇所しかなく、1日の快感の総量がタスク件数で上限になっている。個別トグルを並べる案は 2^N の組合せ爆発と、ADHD ペルソナに対する決定コストの両面で却下し、演出強度をセグメント1本(しずか/ふつう/にぎやか)に集約する。強度で変わるのはタップの光・振動、完了時 confetti、全完了の祝福、朝の環境演出のみ。ドロップカードの尺(T031の不具合修正)と図鑑リプレイは強度に含めない。図鑑リプレイ: /book の記録済みエントリをタップすると DropReveal が replay モードで再生される(ユーザーが能動的にタップした結果なので割り込みではなく、「しずか」でも常時有効)。報酬を新規に増やさずに接触頻度を上げるためレア度インフレが起きず、F-1 にも非干渉。OS の prefers-reduced-motion が ON のときは強度設定より優先して「しずか」を強制する。既定値は「ふつう」 | pnpm verify (lint, typecheck, test, build) incl. 新規 fx ドメインテスト; browser smoke (3段の強度差、図鑑リプレイ、reduced-motion 強制) | 2026-08-16: pnpm verify all green (lint/typecheck clean, 80/80 tests — 62 既存 +8 reminders +10 fx、build 8 routes incl. /settings)。Browser smoke (375x812 mobile, dev server): /settings は EFFECTS/FEEDBACK/ALERTS/ARCHIVE/RESET の5セクションを描画、既定は「ふつう」。強度セグメントは native radio で `labels[0].textContent` が「しずか/ふつう/にぎやか」に正しく紐づく（読み上げ名が value にならない）ことを DOM で確認、「にぎやか」選択で localStorage `fx-intensity=lively` が永続化。ドロップ2件をシードした /book では記録済みセルのみが `aria-label="<名前>の記録を見る"` の button になり（2件）、未記録セルには button なし・tabIndex なし。リプレイ起動後、SURVEY CLEAR ではなく SURVEY RECORD/調査記録より が出て NEW! と台帳行が消え、**5秒後も開いたまま**（RARE1 の通常経路なら4秒で自動消滅）＝自動消滅なしを確認、閉じるボタンで閉じる。tap spark は lively で1ノード生成→1秒後に0（リーク無し）、quiet では0、非対話要素(h1)上でも0。console エラー0件。スモーク用シードと fx-intensity は事後に削除して原状復帰。レビュー修正後に再スモーク: 「にぎやか」でタスクを完了すると QUEST CLEAR 版のリビールが出て**通常経路では自動消滅する**（リプレイの5秒後も開いたままと対比、両モードの差が実機で確認できた）、全完了で 2/2「全クエスト達成！」バナーとストリーク🔥1が出る、/book のリプレイは修正後も SURVEY RECORD で開く、console エラー0件。検証の限界として明記: confetti 2波目のキャンセルは、canvas-confetti が単一 canvas を使い回すため「1波目の描画中」と区別できず、実機での behavioral な証明はできていない（型・lint・build と、同一パターンを book-screen 側でレビュワーが静的確認した結果に依拠）。dev サーバの IndexedDB は tasks/drops/streaks を全消去して空に戻した。 |

| T034 | verified | Claude Code | none | CLAUDE.md, .claude/settings.json, .github/workflows/ci.yml (new), docs/issues.md, docs/repo-map.md, docs/decisions.md, docs/state.md, tasks.md, tests/lib/domain/plant.test.mjs | pm-zero v11.1.1 -> v12 governance migration applied to project scope per D-037 (global ~/.claude already migrated to v12 before this task started): header + Startup Read (docs/issues.md added) + Budget/Continuity (CLAUDE_CODE_AUTO_COMPACT_WINDOW=400000 absolute window, "do not split sessions") + Self-Review (Tier 2 retired) + Self-Evolution (single machine-detectable-check learning loop, docs/lessons.md destination removed) + Git merge-gate (CI green, not self-reported verify) all rewritten in CLAUDE.md; project .claude/settings.json's env (dead CLAUDE_AUTOCOMPACT_PCT_OVERRIDE) and permissions.deny (blanket `.env.*` pattern that blocked reading `.env.example` against documented policy) replaced with a minimal block that defers to the already-v12 global deny + guard.mjs; .github/workflows/ci.yml added running the same lint/typecheck/test/build as pnpm verify, with branch protection requiring it (Section 14 migration item 6, task-plant named explicitly); docs/issues.md stripped to the current-blockers-only template (resolved I-001 entry removed, per Section 9); docs/repo-map.md and tasks.md headers relabeled v12. docs/lessons.md and AGENTS.md do not exist in this repo (migration items 7-8 partially moot). Product source untouched except one test-only timezone fix the new CI itself surfaced (see Evidence) | manual JSON validation of .claude/settings.json; YAML validation of ci.yml; git diff --check; CI green on the PR (first real run of the new workflow) | 2026-08-16: .claude/settings.json parses as valid JSON; ci.yml validated; git diff --check clean. The new CI's first run immediately caught a real pre-existing bug local verify (always run on the owner's JST machine) could never expose: tests/lib/domain/plant.test.mjs's "toLocalDateString is timezone-safe" test asserted a JST-only expectation without pinning the process timezone, so it failed on GitHub Actions' UTC runner (1/80 tests) -- exactly the self-reported-vs-independent-CI gap v12 Section 7 exists to close, so fixed in the same task rather than left red: pinned process.env.TZ = "Asia/Tokyo" in that one test file (toLocalDateString itself is correctly host-timezone-dependent by design for a client PWA's "today"; only the test's determinism was wrong). Re-verified locally with TZ=UTC node --test "tests/**/*.test.mjs": 80/80 pass. CI on the PR green (lint/typecheck/test/build) after the fix, confirming the workflow actually mirrors scripts/verify.mjs rather than merely claiming to. |
| T035 | verified | Claude Code | none | src/lib/notifications.ts, src/components/settings/settings-screen.tsx, src/lib/confetti.ts, src/components/book/book-screen.tsx, tasks.md, docs/state.md | Two user-reported defects fixed: (1) once the browser's Notification permission is granted there is no API to revoke it, so /settings showed a static "通知は有効です" message with no way to turn notifications back off; added an app-level notif-enabled localStorage flag (mirrors sound.ts's fx-enabled) with a Bell/BellOff ON-OFF toggle in the same visual pattern as the FEEDBACK section's sound/haptics switch, gating canNotify() and (re)arming/halting the catch-up pageTimer immediately on toggle. (2) tapping a collected item in /book replayed the exact same fireDropConfetti star-burst used for live task completion, so replay felt identical to achievement; added fireReplayEffect in confetti.ts — a gentler radial circle "twinkle" (not stars, no launch-upward motion) colored per RARE 1-8 rank along the same neutral->green->frost->job-blue->life-purple->ember->deep-ember->gold ramp already used by drop-reveal's RARITY_STYLE — and switched book-screen's handleReplay to call it instead of fireDropConfetti | pnpm verify (lint, typecheck, 80/80 tests, build); browser smoke | 2026-08-17: pnpm verify all green (lint/typecheck clean, 80/80 tests, build 8 routes). Browser smoke via Playwright against the running dev server: granted-state toggle renders Bell/BellOff + ON-OFF pill matching SoundSection exactly, toggling off hides the test-notification button and persists `notif-enabled=0` to localStorage across a reload, toggling back on restores it, 0 console errors throughout. Completed a real quest (bounty auto-claim + task completion) to generate two live drops and confirmed the completion-side QUEST CLEAR reveal is unaffected; navigated to /book and replayed a RARE7 item, confirming SURVEY RECORD copy (unaffected pre-existing behavior) and 0 console errors with fireReplayEffect wired in; test task/drops created during smoke were deleted from IndexedDB afterward to restore prior state. |
| T036 | verified | Claude Code | T035 | src/lib/domain/fx.ts, src/lib/fx.ts, src/lib/domain/rarity-style.ts (new), src/lib/confetti.ts, src/lib/spark.ts, src/lib/sound.ts, src/components/reward/drop-reveal.tsx, src/components/plant/plant-screen.tsx, src/components/settings/settings-screen.tsx, src/components/fx/ (new: grace-particles.tsx, grace-particles-gate.tsx, open-flourish.tsx), src/components/home/home-screen.tsx, src/hooks/use-home-screen.ts, src/components/navigation/bottom-nav.tsx, src/app/layout.tsx, src/app/globals.css, tests/lib/domain/fx.test.mjs, tests/lib/domain/rarity-style.test.mjs (new), docs/decisions.md, docs/state.md, docs/repo-map.md, tasks.md, .claude/rules/pwa-dev-testing.md (new) | Full effects rebuild (D-038, D-039) triggered by two things: a VoC report that T035's replay twinkle "doesn't feel applied" (investigated — deployed correctly, likely too visually subtle vs. the old star-burst) and a large new request for three lively-tier effects (app-open flourish, ambient background particles, per-page transition themes) plus "rebuild every effect in the app." Mid-session Q&A surfaced a same-direction prototype (PR #24) the owner had discarded the day before for being unreachably ambitious/cheap-looking, and led to two governance reversals: D-038 reopens D-024's "no full-screen particles" rule (now opt-in, /plant excluded, snow-level sparseness) and D-039 replaces D-036's quiet/normal/lively dial with 6 independent per-effect toggles (owner's explicit call, ADHD-optimized defaults preserved: 2 pre-existing lightweight effects default on, 4 new heavier ones default off), split into "基本のフィードバック"/"追加の演出" settings sections. IP boundary held: no real trademarked names/assets, only original CSS/SVG-authored effects modeling real games' design language (Elden Ring's sparse upward grace particles, Baldur's Gate 3-style gold ornate framing) plus published game-feel/juice techniques (anticipation, secondary motion, layered easing via the existing --ease-fluid/--ease-out-expo/--ease-spring tokens). Rebuilt: confetti (rarity-style.ts consolidates two previously-separate color ramps; anticipation micro-puff + drift + mixed shapes at high rank), tap sparks (core flash + 2 flung motes), sound (lowpass-sweep timbre, arpeggio jitter, subtle shimmer on save/fanfare, playPage(theme) for the new per-page sound identity), drop-reveal (anticipation hold, spring-settle overshoot at rank4+, light-sweep shine + breathing glow at rank6+, grace-particles behind rank8), /plant snowfall (opacity twinkle + frost-tinted subset). New: grace-particles.tsx shared preset-driven particle primitive (ambient/morning/open/reveal), grace-particles-gate.tsx (ambient background, excludes /plant), open-flourish.tsx (gold-frame + particle arrival moment on open, explicitly NOT icon/logo-centric per the owner's mid-session correction, skippable, non-blocking underneath), and 3 themed page-turn CSS variants (plant=bloom, book=scroll-unfurl, all=ember-flash) layered onto the existing next-view-transitions system via a more-specific data-page-theme attribute selector rather than a new library | pnpm verify (lint, typecheck, 81/81 tests incl. 2 new fx.test.mjs cases replacing the old 3-tier ones + 5 new rarity-style.test.mjs cases, build 8 routes); browser smoke of every new/changed toggle and effect with 0 console errors; Tier 1 fresh-context reviewer (large/behavior-changing diff) before merge; explicit owner sign-off before merge (production auto-deploy) | 2026-08-17: pnpm verify all green (lint clean, typecheck clean, 81/81 tests, build 8 routes). CI green on PR #29 (verify job + Vercel preview). Browser smoke complete via Playwright: settings screen renders both new sections with correct default toggle states (3 on / 4 off); openFlourish/ambientParticles/pageTransitions toggles persist to their localStorage keys on click; under prefers-reduced-motion emulation all 5 stored-true visual toggles show the reduced-motion notice and sound stays unaffected; /plant confirmed to render zero grace-particle DOM nodes even with the toggle on; a real task completion fired the rebuilt confetti burst (mixed star/circle shapes, drift) and a bounty completion fired a RARE-4 drop-reveal, both cleanly, 0 console errors throughout. Open-flourish's mount/dismiss/sessionStorage-once logic confirmed via the storage flag transitioning as expected (its 2.6s window was too fast for MCP round-trip screenshots to catch mid-animation, so its choreography was confirmed by source read instead). Test task and screenshot files cleaned up after. Tier 1 reviewer (Opus 5, fresh context) returned FAIL on one finding plus 4 should-fix items; all 5 addressed: (1) `.drop-reveal-shine` had `animation: none !important` under prefers-reduced-motion but no `opacity: 0`, so a RARE6-8 reveal left a permanent white diagonal streak burned across the card for reduced-motion users — fixed, verified opacity:0 via computed style under emulated reduced-motion. (2) `OpenFlourish`'s sessionStorage claim ran synchronously in the effect body before the deferred setVisible microtask, so React 19 dev/StrictMode's mount->cleanup->mount could claim the token on the doomed first mount and never show the flourish on the second — moved the claim inside the microtask, gated on the same `cancelled` flag. (3) docs/repo-map.md Summary/Entry Points still described the deleted quiet/normal/lively dial (read at every session start) — corrected to the six-toggle model and the new src/components/fx/ directory. (4) No migration from the retired `fx-intensity` key meant a user who had explicitly chosen "quiet" would silently get tapSpark/completion turned back on after this deploy — added a one-shot migration in fx.ts, verified quiet->all-off and lively->all-on via localStorage round-trip in the browser. (5) The new drop-reveal juice (spring-settle, shine, glow, rank-8 particles) was gated only by rarity, bypassing the 完了エフェクト (`completion`) toggle a user may have turned off — now also gated on `isEffectEnabled("completion")`. Also fixed two of the reviewer's minor findings while in the same files: per-page sound identity was silenced by OS reduced-motion via `isEffectEnabled("pageTransitions")` even though sound has its own motion-independent toggle by design — bottom-nav now decides the sound theme from `getStoredEffectPref` instead, leaving only the CSS/visual theme reduced-motion-gated; OpenFlourish's advertised Escape-to-skip and "auto fade-out" didn't work (no autofocus, hard `return null` cut) — added autofocus and a real closing-phase fade transition. Owner reviewed and asked for all remaining findings to be addressed too (plus a UI request: restyle the notification permission button to match the other toggle rows). Fixed 9 more of the 11 remaining minor findings (D-040): tap-spark's `animationend` now listens on the core element, not the group, so the mote's shorter animation no longer truncates the core flash; `OpenFlourish` restructured from `<button>`-wrapping-block-content with a content-suppressing `aria-label` to `<div role="dialog" aria-modal>` + a real focused skip `<button>`, fixing both the invalid markup and the AT-content-suppression the reviewer flagged; `.open-flourish` now pins `--gold` to the bright dark-mode value (its backdrop is always near-black regardless of page theme, so the theme-swapped default was backwards there); `rarityBand()` wired into `sound.ts`'s `playClear` rank branch (replacing a hardcoded `rarity<=3`/`rarity<=6` duplicate of the same ladder) instead of sitting unused; `EFFECT_KEYS`'s stale "ordered for settings-screen display" comment fixed by actually deriving settings-screen.tsx's two row lists from a new `EFFECT_SECTIONS` map instead of a hardcoded duplicate list, with a new test asserting every key has exactly one section (a future 7th key can no longer type-check and ship without a settings row); `fireReplayEffect` and `DropReveal`'s replay-path juice were gated by `isEffectEnabled("completion")`/reduced-motion in a way that contradicted D-036's explicit "図鑑リプレイは常時有効" decision and the settings screen's own footnote — decoupled both from the completion toggle for the replay path specifically, keeping only OS reduced-motion (a distinct accessibility signal) as the gate, matching original intent instead of rewriting the footnote to a lesser promise; plant/all themed page transitions fixed to pin `transform-origin: center center` (they're pure-scale, not directional slides, and were inheriting a directional origin from the base back-navigation rule at equal specificity). Documented as accepted, intentionally unfixed (D-040): the ~40ms window where a screen unmounting between the anticipation puff and the main confetti burst could skip the burst (fixing it would remove the anticipation technique itself), and the loss of former "lively" users' 2-wave confetti capability with no replacement toggle (reintroducing it would partially reopen the 2^N toggle-explosion problem D-039 exists to avoid — an intentional tradeoff, not an oversight). Notification permission's "default" (not-yet-granted) state restyled from a standalone primary CTA button into the same icon-chip+label+pill-switch row pattern used by every other toggle, per explicit owner request. pnpm verify re-confirmed green after all fixes (lint/typecheck/82 tests incl. 1 new EFFECT_SECTIONS coverage test/build 8 routes); targeted browser re-check of the settings screen confirmed 0 console errors and the new notification row rendering correctly. Owner approved merge on 2026-08-17; PR #29 squash-merged to main (1bb8c8e), branch deleted, CI green on the merge commit, Vercel production deployment confirmed via the GitHub deployments API (success). |

| T037 | verified | Claude Code | none | grimore-v2/ (new, 3 docs), docs/decisions.md, docs/state.md, tasks.md | Grimoire v2着手の環境準備(D-041): `chore/v2-env-setup`ブランチを作成し、v2設計資料をリポジトリに追加。決定事項ログP章の5つの機械的セットアップ作業(Blender確認/BlenderMCPアドオン導入/接続確認+PolyHaven連携/Claude Code側MCP登録/CC0四足リグ済みベースモデル調査)を、オーナーの「手順提示ではなく実行せよ」指示に従い自動化可能な範囲で実行 | winget install exit code; blender --background --python の INSTALL_OK/ENABLE_OK/POLYHAVEN_PROP_SET_OK/USERPREF_SAVED/HOMEFILE_SAVED出力; claude mcp list; git diff --check | 2026-08-19: winget install BlenderFoundation.Blender.LTS.4.5 exit 0 (Blender 4.5.10 LTS)。headless python実行で addon.py インストール+enable+userpref保存を確認、PolyHaven連携用scene propertyをTrueにしてstartup.blendへ保存(次回起動時デフォルトON)。`claude mcp add --scope project blender -- uvx blender-mcp` で登録、`claude mcp list` に blender エントリを確認(状態: Pending approval、次回`claude`起動時の標準承認待ち)。CC0四足リグ済みモデルはSketchfab上でほぼ皆無と判明、代わりにQuaternius "Animated Animal Pack"(CC0確認済み、Shiba Inu/Husky/Wolf/Fox含む)を候補として特定・未ダウンロードのまま記録。ソケットサーバー起動("Connect to Claude"クリック)のみBlenderのGUI起動が前提で自動化不可、次回オーナーが手動実施。 |

| T038 | done | Claude Code | T037 | grimore-v2/Grimoire_決定事項ログ_統合版_20260819.md, docs/decisions.md, docs/state.md, tasks.md | Grimoire v2設計相談セッション#1(D-042): これまでclaude.aiで行っていた相談フェーズをClaude Codeへ完全移行し、以後は相談・実装を同一エージェントで担当する。T037/D-041の内容とtasks.md採番ルールとの整合を検証(問題なし)。O章の最優先未決定事項だった犬系グリモ(水属性)の卵〜成体デザインを確定(L-2章に記載)。MHW参照画像の利用方針を、アートディレクション参照限定から背景世界(相棒+世界画面)への直接使用へ転換(A章更新、法的リスクを2回明示提示した上でオーナーが採用) | git diff --check(ドキュメントのみ、Tier 0) | 2026-08-19: git diff --check クリーン(空白エラーなし)。製品コード非変更(grimore-v2/*.md, docs/decisions.md, docs/state.md, tasks.mdのみ)。PR経由でmainへマージ後、CI(lint/typecheck/test/build)がドキュメントのみの差分でも green になることを確認してクローズ。 |

| T039 | done | Claude Code | T037 | docs/decisions.md, docs/state.md, tasks.md | Official Blender MCP(blender.org、2026-04-28リリース)を調査し、乗り換えの是非を検証。得意領域がシーン検査・自動化(現行のahujasid/blender-mcpが得意とする外部アセット生成連携とは別領域)であること、Blender 5.1以降が要件でありT037で選定した4.5 LTSとは合わないことを確認し、当面は乗り換えないと結論。あわせてD-041のBlenderバージョン選定理由(「決定事項ログP章の指示」とのみ記録)を裏取りし、Blender 5.2 LTSがT037実行時点で既に1ヶ月以上前から入手可能だった古い前提であったこと、真の理由はahujasid/blender-mcpのBlender 5.x互換性リスク(5.0でPythonエラー報告あり)であったことをD-041へ訂正として追記(結論=4.5 LTS維持は不変) | git diff --check(ドキュメントのみ、Tier 0) | 2026-08-19: git diff --check クリーン。製品コード非変更。 |
| T040 | done | Claude Code | T038 | grimore-v2/Grimoire_決定事項ログ.md, docs/decisions.md, docs/state.md, tasks.md | Grimoire v2設計相談セッション#2(D-043): O章の未決定事項だった妖精(人型グリモ)の属性を光に確定し、L-3章として新規記載。ビジュアルモチーフ(星屑・プリズム系)、猫(闇/月)との対比構造(陰陽的ペア)、発光エフェクトの使用方針(K章の原則通り適度に使うが非発光手段も併用)を分解質問で確定。植物属性は不採用のためアプリ旧名/plant画面との意味的重複の懸念は解消。卵〜成体の具体的デザインは妖精が最後に着手する系統のため未着手のまま。セッション中にオーナーが設計ログのファイル名を`Grimoire_決定事項ログ_統合版_20260819.md`から`Grimoire_決定事項ログ.md`へ変更(内容は無変更、以後この名前で参照) | git diff --check(ドキュメントのみ、Tier 0) | 2026-08-19: git diff --check クリーン。製品コード非変更。ファイルリネームはオーナー自身の操作であり事故ではないことを確認。 |
| T041 | done | Claude Code | none | CLAUDE.md, docs/decisions.md, docs/state.md, tasks.md; pm-zero repo: pm-zero-knowledge-v12.md(Section 12) | Gitワークフローの2段階見直し(D-044含む)。(1)オーナーの提起を受け、ドキュメントのみ(ソース/設定/CIファイル非変更)の変更はブランチ/PR/CIをスキップしmain直接コミット可という例外をCLAUDE.mdと大元のpm-zero-knowledge-v12.md Section 12の両方に追記(汎用ルールのためpm-zero-knowledge側にも反映)。(2)mainが家族利用中の本番環境(マージで自動デプロイ)である一方、grimore-v2は完成時期未定の長期・投機的な設計+開発作業であるため、`grimore-v2`という長命統合ブランチをmainから新規作成し、以後のv2関連作業(設計相談ドキュメント・将来の実装)はすべてこのブランチを起点・着地点とする方針をD-044として確定、CLAUDE.mdに恒久ルールとして追記 | git diff --check(ドキュメントのみ、Tier 0); git branch/ls-remoteでgrimore-v2ブランチの存在確認 | 2026-08-19: git diff --check クリーン。両リポジトリでmain直接pushを実施(task-plantはブランチ保護のrequired status check "verify"をバイパス済みとGitHubログで確認、意図した挙動)。grimore-v2ブランチ作成・push後にlocal/remoteで存在確認。 |

## Execution Pointer
Current active task, executor, write lock, and latest verification live in docs/state.md.

## Blockers
| ID | Task | Blocker | Needed decision | Owner |
|---|---|---|---|---|

## Review Notes
| Task | Reviewer | Result | Follow-up |
|---|---|---|---|
| T001 | Codex CLI self-audit | pass | Cross-vendor review not triggered; changes are OS docs/scripts and package scripts only. |
| T015 | Claude Code self-audit (Tier 1) | pass | pm-zero v11 governance migration; docs/config only, no product source changed. |
| T016 | Fresh-context Sonnet subagent (Tier 1) | pass | 0 blockers; fixed in follow-up commit: light primary contrast to >=3:1 (WCAG 1.4.11), aria-controls on card expand, empty-state progressbar label, strict HH:MM formatting. pnpm verify re-run green. |
| T017 | Fresh-context Opus subagent (Tier 2: DB schema class) | pass | 0 blockers. Fixed: unique &[taskId+dateKey] index (dedup enforced by store), page-turn timer clearTimeout, AudioContext priming on first pointerdown. Accepted as-is: unused lifetimeCompleted drift (never read by UI; revisit if surfaced). Migration judged data-safe: v3 upgrade recomputes monthly from tasks, no throw path. |
| T018+T021 | Fresh-context Sonnet subagent (Tier 1: 768-line logic diff) | pass | 0 blockers. Fixed: drops field validation in parseBackup (MAJOR: crafted JSON could break bounty evaluation), BACKUP_VERSION guard rejecting newer snapshots, sr-only claimed status on bounty items, aria-hidden on Footprints icon. Accepted as-is: double IndexedDB read per toggle (evaluateBounties refetches all tasks; negligible for a single-user store, revisit if the task list grows large). |
| T026 | Fresh-context Opus subagent (Tier 2: pre-merge, whole branch) | pass — 0 blockers | Data integrity verified mechanically: only 2 deleted lines in drops.ts (the POOL_BY_RARITY[4] rebind and the getDropById find→Map), zero catalog data deletions, all 63 legacy ids byte-identical, LEGACY_DROPS_BY_RARITY matches main's catalog exactly. Confirmed no id has ever changed rarity across the app's whole history, so the new rarity-scoped isNew is backward compatible with the owner's real records. Pity/first-of-day rewrite proven semantically identical to the toArray version (primary-key order + unique autoincrement) across empty table / no-RARE8 / latest-is-RARE8 cases; single-store rw transaction rules out first-of-day and pity races. Applied from the review: untrusted-input guard added to the voice prompt (the one injection path missing it), legacy ids now pin rank as well as existence plus a pool-membership test, one overall time budget for the retry ladder (2 models x 3 payloads x 45s could never fit maxDuration=60), server-side logging of upstream 400 bodies, and the test glob quoted — unquoted it silently ran 8 of 56 tests under a POSIX shell. Accepted as residual: backup-imported records with a malformed rarity escape the isNew check (cosmetic NEW badge only, hand-edited backups only); isNew walks one rarity partition rather than being fully O(log n) (dropId index deferred, D-031). |
| T025 | Fresh-context Opus subagent (Tier 2: security/deploy class) | pass after fixes | 2 pre-merge blockers found and fixed: (1) HIGH — route accepted a free-form {prompt}, making it an unauthenticated open LLM relay on a public URL (up to 6 upstream calls/request, no output cap); replaced with two fixed typed request shapes (kind: "voice" \| "gmail"), prompt-building moved fully server-side, maxOutputTokens capped. (2) README.md:54 still referenced the old NEXT_PUBLIC_GEMINI_API_KEY name; fixed. Also addressed (valuable pre-public-deploy, not blockers): maxDuration=60 added (Gmail extraction's longer upstream retries were exposed to Vercel's default 10s timeout), whole handler wrapped in try/catch (bare fetch/json.parse failures previously fell through to Next's generic 500), API key moved from URL query param to x-goog-api-key header, upstream Gemini error bodies no longer echoed to anonymous callers (logged server-side only). Accepted as residual/deferred: no per-IP rate limit or Origin check (typed-endpoint change already bounds abuse to the app's two real use cases; full rate limiting would need Vercel Firewall or similar, out of scope for a 2-3-person family app). Re-verified: pnpm typecheck/lint/test(36/36)/build all green; curl smoke of both kinds (voice, gmail) + invalid/oversized bodies confirmed 400/502 handling. |
| T028 | Fresh-context Opus subagent (Tier 2: security/deploy class) | pass with nits, fixed | Independently re-verified every claim live (pnpm audit, pnpm why, a real pnpm verify re-run) rather than trusting the diff or ADR text; confirmed shadcn@4.1.2 single-resolved, dist/tailwind.css real and exported, sharp/postcss/nanoid single-version, all 3 brace-expansion lines within their parent minimatch's own declared range. 4 pre-merge-recommended findings, all fixed: (1) major — the brace-expansion parent-scoped overrides keyed on an exact minimatch patch version (e.g. `minimatch@3.1.5>`), which would silently stop matching (and silently re-expose the vulnerable version) the moment minimatch's own resolved patch version shifted on a future install; widened to a major-range selector (`minimatch@3>`). (2)+(3) major — `body-parser` and `ip-address` used bare/global overrides that currently have only one consumer each but would force an incompatible version onto a future second consumer (an express@4 dependency needs body-parser 1.x; ip-address 9.x is common via socks-proxy-agent chains); scoped to their actual parent (`express@5>body-parser`, `express-rate-limit>ip-address`). (4) minor — `@babel/core` and `js-yaml` were split across multiple parent-scoped override keys despite each resolving to a single major-version line tree-wide (confirmed via `pnpm why`); consolidated to one bare override each for simplicity. Documented the scoped-vs-bare decision rule in D-033 so future override additions follow it. Accepted as-is (nits, not fixed): `shadcn` staying in `dependencies` instead of `devDependencies` (would shrink the production install tree further but is out of scope for this pass); the already-scoped-but-technically-unnecessary `ajv>fast-uri` etc. entries (harmless, just inconsistent with the bare-override packages). Re-verified: pnpm audit still 0, pnpm verify all green after the override changes. |
| T027 | Fresh-context Opus subagent (Tier 2: 300+ line diff, whole-branch) | pass with nits, fixed | Independently re-parsed all 436 catalog entries (376 region + 60 garden) and confirmed 0 duplicate ids/names/flavors, every rank pool non-empty per region, RARE4/RARE8 ids/names/photos byte-identical to main, no orphaned-id crash path. 2 major findings fixed: (1) the legacy-id rank-pinning test was deleted with nothing pinning the season-linked RARE4/RARE8 ids that already have real user history, so a future edit could silently break isNew for owned items — added `SEASON_DROPS_BY_RARITY` snapshot test (60 ids) and corrected the now-stale "(pinned by test)" comment in rewardDb.ts. (2) region-accent text on EXPEDITIONS counters failed WCAG AA in light mode for 7/8 regions (as low as 2.11:1) — moved to `text-foreground`, kept accent color on non-text dot/border elements only. 7 minor/nits fixed: D-032 inaccurately claimed RARE4/RARE8 flavor text was untouched (24 of 60 entries' flavor was in fact reworded for the world-atlas frame) — corrected the ADR and added inline comments; long RARE5-8 names could render indistinguishable under `truncate` with no way to see the full name — added `title` + switched to `line-clamp-2`; EXPEDITIONS region strip wasn't keyboard-scrollable — added `tabIndex`; `RegionDef.blurb` was written but never rendered anywhere — wired into the region card's `title`; `getRegionById` threw on an unknown id inside a render path (would white-screen /book on a future data typo) — degraded to a logged fallback instead; regions.ts JSDoc said "seven" expedition regions, actually eight — fixed; a stale pre-rewrite id (`c-kooribana`) lingered in reward-db.test.mjs fixtures — replaced with a real id. Deferred as future work, not fixed now: drop-reveal.tsx doesn't surface the region name at the moment of reveal (only in /book) — low-value UI addition without visual QA time available this session. Re-verified: pnpm verify all green, 58/58 tests (was 57, +1 pinning test). |
| T030 | Claude Code self-audit (Tier 1 — docs/config-only, matches T015's review class) | pass | Governance-only diff (global ~/.claude + project files), no product source touched; substituted Tier 1 self-audit for Tier 2 since no auth/billing/DB-schema/production-data class applies — same substitution basis as T015. |
| T034 | Claude Code self-audit (Tier 0: verify script n/a to docs-only diff, git diff --check; CI is the actual gate per v12) | pass | Governance-only diff (project files + one new workflow), no product source touched; v12 retires Tier 2 outright rather than substituting Tier 1 for it, and this diff is docs/config-only so it also does not meet Tier 1's "300+ line diff / new external API / critical-workflow" trigger — CI green on the PR is the merge gate per the new Git section wording. |
| T031+T032+T033 | Fresh-context Sonnet subagent (Tier 1: 1364/-656 line diff + critical-workflow class — notification delivery rewrite and relocated destructive-reset UI) | changes required -> pass after fixes | 2 MAJOR findings, both real, both fixed. (1) `use-home-screen.ts` discarded the cancel function returned by `fireDropConfetti`/`fireAllCompleteConfetti` at all three home-screen call sites (bounty claim, task completion, all-clear fanfare), while `book-screen.tsx` wired it correctly — so the exact property T033 set out to guarantee was missing at the busiest call site. canvas-confetti draws onto document.body outside React, so at にぎやか (2 waves, 450ms apart) navigating away within the gap dropped the second wave onto the next screen. Fixed at the module level instead of per call site, since a per-burst handle is easy to forget: `confetti.ts` now tracks every pending wave in a module-level Set and exposes `cancelPendingConfetti()`, and the home hook clears it on unmount. (2) `pageTimer` only ever armed for a reminder still in the future, and the 09:00 slot occurs once a day, so after a catch-up delivery `nextPending()` returns null and no timer is set — meaning an installed PWA that is resumed rather than reloaded (the normal case) would never check the next morning's slot. No `visibilitychange`/`pageshow` listener existed anywhere. Added a `visibilitychange` listener that re-runs the catch-up whenever the app returns to the foreground. Also fixed 1 MINOR (concurrent `syncTaskNotifications` calls could both read the ledger before either wrote it; previously masked by Notification `tag` coalescing, now genuinely serialised through a promise chain) and 1 NIT the branch itself introduced (`prefersReducedMotion` existed twice — the older `view-transition.ts` copy now re-exports the guarded `fx.ts` one). Reviewer confirmed clean: reveal timing/propagation/close button, ledger corruption + unbounded-growth resistance, timezone handling, `domain/*.ts` relative-import rules, byte-identical migration of the two-tap destructive reset confirm, /book data refetch on navigation making the dropped callbacks safe, one-time notification prompt guards, reduced-motion precedence on every motion path, spark DOM-leak safety, `.morning-light` stacking, BottomNav null path, sw.js handler removal vs old cached workers, and Japanese copy matching the new catch-up semantics. |
| T029 | Fresh-context Sonnet subagent (Tier 1: critical-workflow class — irreversible user-data deletion) | pass after fix | Checked 6 areas against the diff: blast radius/atomicity (clean — `resetCalendar`'s `db.transaction("rw", db.tasks, db.streaks, ...)` and `resetSurveyNotes`'s `db.drops.clear()` touch exactly the documented tables, no `plantState` leak), UI safety (two-tap confirm can't be bypassed, cancel is a true no-op, confirm button correctly `disabled={busy}`), error handling (1 bug, see below), test quality (both new/extended test files exercise real Dexie writes via fake-indexeddb and assert cross-table isolation), consistency with `task-edit-modal.tsx`'s existing delete-confirm pattern (matches, no a11y regression). 1 real bug found and fixed: `ResetRow`'s `handleConfirm` catch block set an error message but never kept the confirm panel open in a state that rendered it (message only rendered in the collapsed `!confirming` branch, and `confirming` was never set back to true/kept true on failure), so a failed delete showed zero feedback to the user — added a second `aria-live` message slot inside the still-open confirm panel for the failure path. 1 accepted-as-is nit: the count shown in the warning panel is snapshotted at the first tap and not re-checked at the second tap (informational number could be slightly stale in a multi-tab race); deletion scope itself is still "all rows" as documented, so not a correctness bug. Re-verified: pnpm verify all green, 62/62 tests. |
